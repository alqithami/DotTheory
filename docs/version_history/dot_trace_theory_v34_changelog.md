# Dot-Trace Theory v0.34 Changelog

## Revision focus
This revision is a front-matter coherence, first-40-pages flow, and figure-caption polish pass. It does not add new theoretical mechanisms. Its purpose is to make the manuscript read more like a mature academic theory article in the sections that establish reader confidence.

## Main changes

1. Rewrote the abstract for stronger academic flow and a clearer statement of the conditional representational claim.

2. Rewrote the introduction around a clean problem sequence:
   - persistent traces of prior interaction,
   - limits of agent-edge-only representation,
   - definition of the dot,
   - definition of the dot field,
   - the central coupling claim.

3. Rebuilt the conceptual overview figure to remove arrow-label overlap and make the diagram visually legible.

4. Revised the contributions and scope section so it reads as a stable contribution statement rather than a developmental checklist.

5. Revised the theoretical-scope section and article-structure paragraph for a more professional transition into related work and formal sections.

6. Tightened the notation and state-hierarchy section by removing wording that acknowledged local notation inconsistency and by reinforcing the distinction between dot-dot relations and retrieved dot sets.

7. Improved the informal dot definition by replacing a long bullet list with a compact prose paragraph that introduces examples without interrupting flow.

8. Corrected the ontology notation for accessible dots and retrieved dots:
   - accessible dots: `\mathcal M_i(t)`
   - retrieved dots: `\mathcal V_i(q_i,t)`

9. Rewrote the dot-field section to remove an ASCII/verbatim diagram and replace it with polished prose and a formal feedback equation.

10. Revised the integrated mixed-mechanism dynamics opening so it reads as a formal model integration rather than a development-stage explanation.

11. Renamed the earlier "minimal integrated model for the formal article" to "reduced integrated model" and rewrote the surrounding prose to remove draft-process language.

12. Rewrote the recursive topology-feedback stability opening to remove internal development phrasing and clarify why a formal stability analysis is needed.

13. Added explicit input/output lines to the canonical dot-field transition algorithm and the dot-extraction algorithm.

14. Expanded and polished figure and table captions so they are more self-contained and interpretive.

15. Recompiled the LaTeX with BibTeX-compatible bibliography processing and rendered representative pages for visual inspection.

## Validation checks

- PDF compiled successfully under pdfLaTeX with bibtex8.
- No unresolved citation or reference warnings in the final compile log.
- Representative pages from the opening sections, figures, algorithm blocks, and stability section were rendered and visually inspected.
- Development-stage phrases such as `TODO`, `TBD`, `placeholder`, `next step`, `minimum publishable`, `publication strategy`, `working paper`, and `formal article` were checked and not found in the LaTeX source.
